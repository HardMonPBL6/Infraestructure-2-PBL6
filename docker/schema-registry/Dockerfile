# Confluent Schema Registry — PATRÓN: extender imagen oficial.
# El heap topado y la conexión a Kafka los inyecta el deploy vía env
# (schema_registry_* en group_vars). Aquí solo config estática del equipo.
FROM confluentinc/cp-schema-registry:7.6.1

EXPOSE 8081
