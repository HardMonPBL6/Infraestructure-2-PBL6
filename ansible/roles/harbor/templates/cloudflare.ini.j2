# {{ ansible_managed }}
# Credenciales para el plugin certbot-dns-cloudflare (reto DNS-01 de Harbor).
# Token con permiso Zone:DNS:Edit sobre la zona de Harbor.
dns_cloudflare_api_token = {{ harbor_cloudflare_dns_token }}
